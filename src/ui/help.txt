claude-session preserves native claude behavior while adding isolated sessions.

Unknown arguments are forwarded unchanged. Use `--` to force every following
argument to the child. Leading `account`, `help`, `version`, and `doctor` are
wrapper-owned. `account login [name]` delegates saved-login setup to the child;
`account list` reports local account usability. Unknown and unimplemented
surfaces remain native passthrough.
