# cargo-dist configuration; regenerate .github/workflows/release.yml.
[workspace]
members = ["cargo:."]

[dist]
ci = "github"
installers = ["shell"]
targets = ["x86_64-unknown-linux-gnu"]
cargo-dist-version = "0.32.0"
install-path = "CARGO_HOME"
install-updater = false
