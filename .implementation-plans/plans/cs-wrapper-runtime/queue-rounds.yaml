# Rounds for this plan, in execution order. status: backlog | todo | doing | done
rounds:
  - item: child-resolution
    status: todo
    depends_on: []
    prompt: /prex -ar .implementation-plans/plans/cs-wrapper-runtime/child-resolution.md
    notes: Spawner trait, child-binary resolution chain, recursion guard, version verb.
  - item: spawn-signals-exitcodes
    status: todo
    depends_on:
      - child-resolution
    prompt: /prex -ar .implementation-plans/plans/cs-wrapper-runtime/spawn-signals-exitcodes.md
    notes: Spawn-and-wait, signal forwarding, child exit-code mapping; replaces minimal spawn.
  - item: child-env-injection-and-headroom-seam
    status: todo
    depends_on:
      - spawn-signals-exitcodes
    prompt: /prex -ar .implementation-plans/plans/cs-wrapper-runtime/child-env-injection-and-headroom-seam.md
    notes: Ordered ChildEnv scrub + CLAUDE_CONFIG_DIR + argv-prefix seam + byte-recording stub + e2e passthrough tests.
